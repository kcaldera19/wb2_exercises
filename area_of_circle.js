// defining variable 
let radius = 3;

//calcluations for the variables above
let areaOfCircle = Math.PI * (radius ** 2);

// output the results of the variable above
console.log("The area of a circle with radius of 3 " + areaOfCircle)