// variables created as inputs for calculation
let interestRate = 3.5;
let savings = 300.00;

let year = 72 / interestRate;

// output of how many years to double 
 console.log("At a 3.5% "+ interestRate+ "your saving account will be worth $600.00 in "+ year+" years" )

// fixed 2 digit decimal point
console.log(year.toFixed(2))

// fixed 1 digit deciaml point
console.log(year.toFixed(1))
